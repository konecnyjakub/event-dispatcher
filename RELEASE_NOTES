A new version of Event Dispatcher is out. It is possible to register multiple listeners at the same time in PriorityListenerProvider with registerListeners. A new event dispatcher for debugging was added. Support for event subscribers was added.

For complete list of changes since previous version, see CHANGELOG.md.
