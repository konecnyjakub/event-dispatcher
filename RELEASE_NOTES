A new version of Event Dispatcher is out. It is possible to register multiple listeners at the same time in PriorityListenerProvider with addListeners. A new event dispatcher for debugging was added. Support for event subscribers was added. ListenerProvider was deprecated in favor of  PriorityListenerProvider (they provide almost same functionality).

For complete list of changes since previous version, see CHANGELOG.md.
