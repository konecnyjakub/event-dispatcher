A new version of Event Dispatcher is out. It adds a new dummy event dispatcher. Method DebugEventDispatcher::dispatched was fixed, it sometimes returned incorrect results. EventDispatcher can now log dispatched events, just pass a PSR-3 logger as second parameter to its constructor.

For complete list of changes since previous version, see CHANGELOG.md.
