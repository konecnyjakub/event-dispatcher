A new version of Event Dispatcher is out. It is possible to register multiple listeners at the same time in ListenerProvider with registerListeners.

For complete list of changes since previous version, see CHANGELOG.md.
