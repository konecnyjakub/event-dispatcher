A new version of Event Dispatcher is out. ChainListenerProvider::registerProvider was deprecated in favor of new addProvider(). DebugEventDispatcher can now check if an event of a certain type was dispatched.

For complete list of changes since previous version, see CHANGELOG.md.
