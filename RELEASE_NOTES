A new version of Event Dispatcher is out. It adds a new dummy event dispatcher. Method DebugEventDispatcher::dispatched was fixed, it sometimes returned incorrect results.

For complete list of changes since previous version, see CHANGELOG.md.
